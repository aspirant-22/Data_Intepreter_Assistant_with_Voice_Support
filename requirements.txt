Flask==2.3.3
flask-cors==4.0.0
SpeechRecognition==3.10.1
pyttsx3==2.90
pyaudio==0.2.13

Note: If you had issues installing pyaudio, you might need to install it via wheel or use an alternative like sounddevice for audio input.
